This directory is typically used to store user-script config files, peristence files etc. 
The files in this directory is accessable from the blocks APIs. 
Any example files should be kept in the files-archive directory to avoid unexpected overwrites.